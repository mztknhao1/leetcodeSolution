#include "greedy.h"

int main(){
    // vector<int> cost = {259,770};
    // vector<vector<int>> costs;
    // costs.push_back(cost);
    // cost[0] = 448;
    // cost[1] = 54;
    // costs.push_back(cost);
    // cost[0] = 926;
    // cost[1] = 667;
    // costs.push_back(cost);
    // cost[0] = 184;
    // cost[1] = 139;
    // costs.push_back(cost);
    // cost[0] = 840;
    // cost[1] = 118;
    // costs.push_back(cost);
    // cost[0] = 577;
    // cost[1] = 469;
    // costs.push_back(cost);
    // int result_q1029 = twoCitySchedCost_q1029(costs);

    // vector<int> point = {-173,399};
    // vector<vector<int>> points;
    // points.push_back(point);
    // point[0] = 62;
    // point[1] = -213;
    // points.push_back(point);
    // point[0] = 71;
    // point[1] = 282;
    // points.push_back(point);
    // point[0] = -45;
    // point[1] = 851;
    // points.push_back(point);
    // point[0] = 710;
    // point[1] = 982;
    // points.push_back(point);
    // point[0] = 493;
    // point[1] = 985;
    // points.push_back(point);
    // point[0] = -529;
    // point[1] = -946;
    // points.push_back(point);  
    // point[0] = -83;
    // point[1] = 78;
    // points.push_back(point);  
    // point[0] = 624;
    // point[1] = -785;
    // points.push_back(point);  
    // point[0] = 877;
    // point[1] = -351;
    // points.push_back(point);  
    // point[0] = 500;
    // point[1] = -376;
    // points.push_back(point);  
    // point[0] = -601;
    // point[1] = -305;
    // points.push_back(point);  
    // point[0] = -23;
    // point[1] = -79;
    // points.push_back(point);  
    // point[0] = 680;
    // point[1] = 204;
    // points.push_back(point);  
    
    // vector<vector<int>> result_q973 = kClosest_q973(points,6);

    vector<int> stones = {7,3,8};
    int ans_q1046 = lastStoneWeight_q1046(stones);


    return 0;
}